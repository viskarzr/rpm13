<Window x:Class="rpm13.WinPass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:rpm13"
        mc:Ignorable="d"
        Title="Авторизация" Height="300" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Activated="Window_Activated">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="20">
            <Label Content="АВТОРИЗАЦИЯ" HorizontalAlignment="Center" HorizontalContentAlignment="Center" FontSize="20" FontWeight="UltraBlack"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="5">
            <Label Content="пароль:" HorizontalAlignment="Center"/>
            <PasswordBox x:Name="txtPass" Width="200" PasswordChar="*"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="5">
            <Button x:Name="btnEnter" Content="Войти" Width="200" Margin="5" IsDefault="True" Click="btnEnter_Click"/>
            <Button x:Name="btnEsc" Content="Отмена" Width="200" Margin="5" IsCancel="True" Click="btnEsc_Click"/>
        </StackPanel>

    </Grid>
</Window>
